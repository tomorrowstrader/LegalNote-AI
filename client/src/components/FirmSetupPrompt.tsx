// FirmSetupPrompt is retired — firm profile completeness is now managed by
// FirmCompletenessChecklist in the Settings page (Settings.tsx).
// This component renders nothing; it is kept for import compatibility only.
export default function FirmSetupPrompt() {
  return null;
}
